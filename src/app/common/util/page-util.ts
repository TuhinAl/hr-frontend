import { Page } from '../model/page';
import {addPropToObj} from './object-util';

/*

export function makePage<T extends object>(listOfObj: Array<T>,indexOfPage:number =0,elementPerPage:number = 10): Page<T> {

  listOfObj.length
  return new Page({content:[]})
  return new Page({content:[]})
}
*/



